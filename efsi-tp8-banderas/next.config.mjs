// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['upload.wikimedia.org'], // Agrega los dominios permitidos aquí
    },
  };
  
  export default nextConfig;
  